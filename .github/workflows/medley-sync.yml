name: Automatisert Medley Oppdatering

on:
  schedule:
    - cron: '0 6 1,15 * *' # Kjører kl. 06:00 den 1. og 15. hver måned
  workflow_dispatch: # Tillater manuell start fra Actions-fanen

jobs:
  build-and-sync:
    runs-on: ubuntu-latest

    steps:
      - name: Sjekk ut kildekode
        uses: actions/checkout@v4

      - name: Sett opp Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Installer avhengigheter
        run: npm install axios cheerio

      - name: Kjør Medley-skraper
        run: node scraper.js

      - name: Lagre endringer i repository
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add data/swimmers.json || true
          git commit -m "Auto-oppdatert svømmedata fra Medley.no" || exit 0
          git push
